@model Lynicon.Models.Link
@using Lynicon.Models
@using Lynicon.Utility
@if (Model != null)
{
<div class='lyn-link'>
    <div class='lyn-link-url'>
        <span class="editor-label">Url:</span>@Html.EditorFor(l => l.Url)
    </div>
    <div class='lyn-link-content'>
        <span class="editor-label">Content:</span>@Html.EditorFor(l => l.Content)
    </div>
</div>
}
@Html.RegisterLocalStyles("link-local-styles", @"
#editPanel .lyn-link .text-display, #editPanel .lyn-link .text-box { width: 70%; }
")
